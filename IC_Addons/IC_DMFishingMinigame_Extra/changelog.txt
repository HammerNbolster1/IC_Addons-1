14 September 2026 - v0.2.2
 - Improving impossible seat detection to account for forced champions.
 - Adding the option to fish regardless of whether you have chosen an impossible acceptable seat or not. Just-in-case the script is wrong.
 - Adding an x under acceptable seat checkboxes if they are impossible rather than disabling them.
14 September 2026 - v0.2.1
 - Fixing some UI update bugs.
14 September 2026 - v0.2.0
 - Adding checks for which seats it is possible for DM to pick.
13 September 2026 - v0.1.3
 - Attempting to fix a potential race condition with reading DM's seat. It appears that his Special Guest Star is sometimes not chosen by the time the script wants to read it. It now has a timeout of 5 seconds to read his seat.
 7 September 2026 - v0.1.2
 - Changed it so that `TextSearch` (previously `FindText`) mode is now the default mode.
 - `Default` mode has been renamed to `Formulaic`.
 7 September 2026 - v0.1.1
 - Adding a mechanism where the script will try to reopen the Complete Adventure dialogue if clicking it takes too long.
 - If it continues to take too long it will close every dialogue with Shift+Esc and try again.
 5 September 2026 - v0.1.0
 - Hotfixing two bugs with FindText mode.
 - And I am now satisfied with the state of the addon to call it v0.1.0.
 5 September 2026 - v0.0.7
 - Adding a `FindText` mouse coordinates mode. This uses image search so it could be more reliable than `Default`. It is definitely faster when it works.
 - Made is so that the `Opening Complete Adventure` stage will retry to open that dialogue half way through the timeout.
 - Slightly increased the timeout for the `Opening Complete Adventure` stage.
 5 September 2026 - v0.0.6
 - Improving the Default coordinate calculations so hopefully they work on more resolutions.
 4 September 2026 - v0.0.5
 - Fixing a bug where the game window hwnd would not update if the game was reloaded after Script Hub had been loaded.
 4 September 2026 - v0.0.4
 - Adding sanity checking for Custom mouse coordinates.
 - Improving timeout details in the Status.
 4 September 2026 - v0.0.3
 - Filled out the README.md.
 - Fixed a bug where the script wouldn't re-open the process reader - causing failed memory reads for a new game instance.
 - Fixed a bug where `Num Resets` field wasn't displaying.
 - Fixed a bug where not all elements were being disabled during running when they should have been.
 - Removed the `Test` button and put a `Check Seat Now` button.
 - Changed the wording of a few elements.
 4 September 2026 - v0.0.2
 - Adding a dropdown that lets you choose between Default mouse coordinates and Custum mouse coordinates. For when the Default ones don't work.
 3 September 2026 - v0.0.1
 - Initial Release. An addon that will retry an adventure until DM picks the Guest Star seat that you want.